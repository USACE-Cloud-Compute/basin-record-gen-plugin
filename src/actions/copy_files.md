# Copy Files Action

Utility actions that allow you to copy inputs into the running plugin and copy outputs to a datastore.  Currently only looks at `payload.inputs` for `copy-inputs` and `payload.outputs` for `copy-outputs`.