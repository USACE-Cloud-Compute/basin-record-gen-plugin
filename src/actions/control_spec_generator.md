# Control Specification Generator Action

Makes a copy of an existing control specification and changes its start time and end time dependent on an event date and duration with additional options for padding the event in either direction.  Places the result in a folder in the HMS project's data folder

__Not yet implemented as a top-level action but used by the `process-basin-files` action__